# 阶段 5 验收记录

> 填写方式：把「实际结果」列替换成你机器上的真实输出。定时器精度的测量值请如实填写，
> 漂移比预期大也是结论，不要改成理想值。

## 一、环境信息

| 项 | 命令 | 实际结果 |
| --- | --- | --- |
| CPU 与内存 | `nproc`、`free -h` |  |
| 系统与内核 | `cat /etc/os-release \| head -2`、`uname -r` |  |
| 编译器 | `g++ --version \| head -1` |  |
| 启动参数 | `./build/webserver -t 4 -i 5 -p 8080` |  |

## 二、功能验收

| 序号 | 检查项 | 命令 | 期望结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | 空闲超时生效 | `nc 127.0.0.1 8080`（连上后不输入，等待） | 5 秒左右被服务端断开，日志出现 `[timeout]` |  |
| 2 | 有数据往来不误杀 | 用 `nc` 每 2 秒发一行，持续 20 秒 | 连接保持，不出现 `[timeout]` |  |
| 3 | 主动断开立即关闭 | `curl -i http://127.0.0.1:8080/index.html` 后观察 | 短连接正常关闭，不依赖超时 |  |
| 4 | 定时器取消生效 | 建立连接后立即断开，等待 5 秒 | 不会出现该 fd 的 `[timeout]` 日志 |  |
| 5 | 周期任务 | 观察 30 秒 | 每 10 秒打印一次 `[stats] online=... timers=...` |  |
| 6 | 状态接口 | `curl -s http://127.0.0.1:8080/api/status` | JSON 含 `idleTimeoutSeconds` 与 `timers` |  |
| 7 | 关闭超时功能 | 不带 `-i` 启动，连上后等待 | 不会出现 `[timeout]`，连接保持 |  |

## 三、精度测量

用下面这条命令测量 10 次，记录实际断开时间（秒）：

```bash
for i in $(seq 1 10); do
  start=$(date +%s.%N)
  (nc 127.0.0.1 8080 < /dev/null &) ; sleep 0.2
  # 观察服务端日志时间戳或客户端报错时间，记录断开时刻
done
```

| 次数 | 设定超时 | 实际断开 | 偏差 |
| --- | --- | --- | --- |
| 1 | 5s |  |  |
| 2 | 5s |  |  |
| 3 | 5s |  |  |

结论（例如：偏差在 ±100 毫秒以内，符合把最近到期时间交给 epoll_wait 的预期）：

## 四、对吞吐的影响

| 配置 | 并发 | QPS | P99（毫秒） | 备注 |
| --- | --- | --- | --- | --- |
| `-t 4`（无定时器任务） | 200 |  |  | 阶段 4 基线 |
| `-t 4 -i 5` | 200 |  |  | 每个连接一个空闲定时器 |
| `-t 4 -i 5` 且开启 worker | 200 |  |  |  |

## 五、并发与竞态检查

| 检查 | 命令 | 期望 | 实际 |
| --- | --- | --- | --- |
| 定时器与连接关闭并发 | `-t 4 -i 2` 下持续压测并强制断开客户端 5 分钟 | 无崩溃、无 `[timeout]` 访问已释放连接 |  |
| ThreadSanitizer | `cmake -B build-tsan -DCMAKE_CXX_FLAGS="-fsanitize=thread"` 后同场景 | 无数据竞争报告 |  |
| fd 泄漏 | 压测后 `ss -tn state established \| wc -l` | 回到接近 0 |  |

## 六、结论与下一步

结论：

| 问题 | 影响 | 计划 |
| --- | --- | --- |
|  |  |  |

下一步：阶段 6 引入日志系统后，把当前的 `std::cout` 输出统一替换为分级日志，
并观察日志对 QPS 的影响是否在可接受范围内。
